# Auxiliary alarm system for motor
this system aims to detect potentially dangerous vehicle which doesn't be notticed by rider.

## Hardware:
  * EdgeTPU(Dev. Board): obect detection, object tracking, and collide prediction

